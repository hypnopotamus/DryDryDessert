import type { navigateToHome } from "../App";

interface props {
    navigateToHome: navigateToHome;
}

export const ShoppingCart = ({ navigateToHome }: props) => {
    return (
        <div>cart</div>
    );
};